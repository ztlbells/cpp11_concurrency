Part 3
1. dangling references and thread stacks
2. races
	- multiple threads are racing to access the same variable
	- current thread: (std::this_thread)
3. taking turns
	- thread creation and join as synchronization points
	- introduction to "happens before"
	- unique references

* Each thread has its own stack.

4. duration is defined in head file chrono.h

5. std::thread_th(&func, std::move(list));
	- list to move to thread, main cannot get access to list any longer.